services:
  - type: web
    name: diabetes-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn diabetes_project.wsgi:application
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: diabetes_project.settings
      - key: SECRET_KEY
        value: ta-cle-secrete-ici
      - key: DEBUG
        value: False